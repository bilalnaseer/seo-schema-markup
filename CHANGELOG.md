# Changelog

All notable changes to this project. Newest first. Agents: **append an entry here for every meaningful change** so others don't have to re-read the codebase. Format loosely follows [Keep a Changelog](https://keepachangelog.com/); dates are YYYY-MM-DD.

---

## [Unreleased]

### Added
- **Permalink auto-slugified.** `tools/build-blog.js` now runs the Permalink field / filename through a proper `slugify()` (lowercase, non-alphanumerics collapsed to single hyphens, leading/trailing hyphens trimmed) — e.g. pasting `My New Blog Post!` yields the URL `/blog/my-new-blog-post/`. Sveltia CMS can't live-transform a string field as you type, so the conversion happens at build; the field hint now says so. Replaces the older inline `.replace(/[^a-z0-9-]/gi,'-').toLowerCase()` (which didn't collapse/trim). Also regenerated `/blog/` (removed the stale orphan `welcome-to-the-blog/` page and refreshed the index to match current `content/blog/`).
- **Editable blog permalink + new author defaults (CMS).** `admin/config.yml`: added an optional **Permalink (URL slug)** field (`slug`) to the blog collection so editors can set a custom post URL — `tools/build-blog.js` already honors a front-matter `slug` override (falls back to the title-derived filename when blank, sanitized to `[a-z0-9-]`), so no build change was needed. Changed the default **Author name** to `seoschemamarkup-admin` and default **Author URL** to `https://seoschemamarkup.com` (were `Bilal Naseer` / `https://websensepro.com`). Only affects new-post defaults in the editor; existing posts and the `DEFAULT_AUTHOR` fallback in `build-blog.js` are unchanged. Verified: config contains all three changes and `build-blog.js` still parses.
- **Blog post aggregate ratings (owner-approved §5 exception).** Blog posts can now carry a manually-entered `aggregateRating` set by the content team in the CMS. New optional front-matter `rating_value` / `rating_count` / `best_rating` (added as number fields in `admin/config.yml` with a prominent warning hint). `tools/build-blog.js` nests an `AggregateRating` inside the post's `BlogPosting` JSON-LD **only** when a value is present and count > 0 (blank ⇒ omitted, preserving the "omit when 0" half of §5), and renders the rating **visibly** on the post (`.post-rating` in `main.css`) — required so the on-page value matches the schema. The site owner explicitly accepted the Google manual-action risk after being warned that self-entered ratings violate Google's guidelines and that `AggregateRating` is not a supported rich result on `Article`/`BlogPosting`; documented as a scoped exception in AGENTS.md §5. Verified: rating omitted with no fields, and emits valid `BlogPosting.aggregateRating` + a visible line when supplied.
- **`build-blog.js` now clears `/blog/` before each build** so posts deleted or renamed in the CMS no longer leave stale orphan pages live (everything under `/blog/` is generated, so the wipe is safe).
- **Blog + CMS (`/blog/` + `/admin/`).** New team-editable blog. Non-technical editors write posts in **Sveltia CMS** at `/admin` (vendored bundle `admin/sveltia-cms.js`, pinned `@sveltia/cms@0.172.4`, self-hosted — no CDN, honoring §3; the admin page is `noindex`). Config in `admin/config.yml`: GitHub backend, **editorial workflow** (Draft → In review → Ready, approval required), media in `assets/img/blog/`. Posts are Markdown in `content/blog/*.md` (front-matter: title, description, date, updated, author, author_url, image, tags, draft). New **`tools/build-blog.js`** turns each post into a static, crawlable page at `/blog/<slug>/` and builds the `/blog/` index — with `BlogPosting` + `BreadcrumbList` JSON-LD per post and `Blog` JSON-LD on the index. It uses a **self-contained, zero-dependency Markdown parser** (headings, bold/italic, links w/ auto external `rel`+`target`, inline code, fenced code, images, ordered/unordered lists, blockquotes, hr) plus a minimal front-matter parser; `require.main` guard so `require()` from the sitemap script doesn't trigger a build. Shared nav/footer/head were **extracted into `tools/chrome.js`** (single source now used by BOTH `build-pages.js` and `build-blog.js`); `build-pages.js` output is byte-identical except the new **"Blog"** link added to nav + footer. Added the "Blog" link to the 3 hand-written pages (`index.html`, `schema-markup-validator/index.html` nav+footer; `404.html` popular-tools grid) per §3. `build-sitemap.js` now includes `/blog/` and every post with **real per-post `lastmod`** from front-matter and `changefreq: weekly` (17 → 20 URLs). Small blog CSS added to `main.css` (post meta, cover image, blog cards) reusing existing `.prose`/`.grid`/`.card`. Sample post `content/blog/welcome-to-the-blog.md`. Full dashboard/OAuth/Worker/Cloudflare setup documented in **`BLOG-SETUP.md`**. Verified: all tool scripts parse, all blog JSON-LD blocks valid, generated post HTML renders correctly, sitemap includes blog URLs.
- Embedded the WebSensePro YouTube walkthrough (`https://youtu.be/MNkA_IapQm4` — "Shopify SEO - How To Add Schema Markup [FREE - Without APP]") into the **`/shopify-schema-markup/`** guide, right after the intro, using the same `.video-embed` figure + `extraJsonLd` `VideoObject` pattern as the how-to guide. Real values fetched from the video: `uploadDate` 2026-07-16, `duration` `PT16M29S`, maxres + hq `thumbnailUrl`, `contentUrl`, `embedUrl`, WebSensePro publisher. Sitemap regenerated (still 18 URLs). Verified: both JSON-LD blocks (BreadcrumbList + VideoObject) parse.
- Embedded the WebSensePro YouTube walkthrough (`https://youtu.be/sG3BkcOQTq0` — "How to Add Schema Markup to Your Website [SEO For Beginners]") into the **`/how-to-add-schema-markup/`** guide, right after the intro. Added a responsive `.video-embed` figure (16:9 `aspect-ratio` frame, `loading="lazy"` iframe, no layout shift, dark-mode aware) to `assets/css/main.css`, and rendered it via the guide `body` in `tools/pages-data.js`. Also added a genuine **`VideoObject`** JSON-LD block to that page's `<head>` via a new `extraJsonLd` field on the guide config (the `guidePage()` template already appended `p.extraJsonLd`, so no template change). All VideoObject values are real (fetched from the video): `name`, `description`, `thumbnailUrl` (maxres + hq), `uploadDate` (2026-07-09), `duration` `PT11M55S`, `contentUrl`, `embedUrl`, and a `WebSensePro` publisher. Sitemap regenerated (still 18 URLs; `how-to-add-schema-markup` lastmod bumped). Verified: `pages-data.js` parses, both JSON-LD blocks on the page (BreadcrumbList + VideoObject) parse as valid JSON.
- New generator **`/shopify-product-schema-generator/`** — a Shopify-specific Product schema tool. Unlike the other 8 generators (form → static JSON-LD, validated by the shared engine), this one emits a ready-to-paste **Liquid** snippet whose values resolve live on the storefront (`{{ product.title | json }}`, price, availability, variants, ratings) — one snippet covers the whole catalog. New thin module `assets/js/generators/shopify-product.js` with its own self-contained mount (it can't reuse `core.js`/`validateObject` because Liquid `{{ }}` isn't valid JSON); toggles/selects choose which properties are included and Liquid `{% if %}` guards decide per-storefront presence. Options: offers as one-Offer-per-variant / single Offer / AggregateOffer price range; optional brand (vendor), description, SKU, category (product type), GTIN (barcode) and all-images. **Ratings** are entered as three text inputs — average rating, review count, best rating — as Liquid/metafield expressions (prefilled with Shopify's native `reviews` metafields; merchants swap in their own reviews-app metafields). The `aggregateRating` is wrapped in an `{% if <review count> > 0 %}` guard so it only renders with genuine reviews (constraint §5/§7), and is emitted only when both rating and count are supplied. The **entire snippet is wrapped in `{% if template.name == 'product' %}`** so it emits on product pages only. Added a home-page tool card (🛍️) after Product Schema in `index.html`. To support the Liquid output, `tools/build-pages.js` `generatorPage()` gained a `p.liquid` branch (panel heading “Liquid code for your theme”, “Copy Liquid” / “Download .liquid” buttons, no “Copy `<script>`”). New page config in `tools/pages-data.js` (`liquid: true`), with a 300–500-word explainer and cross-links to the Product generator, Shopify guide and AggregateRating guide. Added to the **Generators** nav dropdown + footer (after Product Schema) in `tools/build-pages.js`, `index.html` and `schema-markup-validator/index.html`; `404.html` (curated grid, no dropdown) unchanged. New `.field-check` CSS for checkbox rows in `main.css`. Sitemap regenerated (17 → 18 URLs). Verified: all JS parses, and the emitted Liquid produces valid `Product` JSON-LD across all 62 toggle combinations (offer mode × ratings app × images × reviews present/absent) via a simulated storefront render.
- Animated moving-light border on the hero `.badge` ("100% Free · No signup · Open source"): a bright accent segment continuously travels around the pill's border. Built with a rotating `conic-gradient` masked into a transparent border via `padding-box`/`border-box` background layers, animated smoothly with a registered `@property --angle` (`badgeBorderSpin`, 3s linear loop). Combined with the badge's existing `heroRise` entrance animation via a comma-separated `animation` list; disabled under `prefers-reduced-motion`. CSS-only, no layout shift (border sits on an already `border-box` element).
- Homepage hero animations (CSS-only, in `assets/css/main.css`): staggered fade-up entrance for the badge, H1, lead and CTA row (`heroRise` keyframe); a subtle pulsing radial-gradient glow behind the hero (`heroGlow`, decorative `::before`, `overflow:hidden` on `.hero` so it never causes horizontal scroll); and a gentle hover-lift + shadow on the hero CTAs. All transform/opacity only (GPU-friendly, no layout shift / no CLS), fully disabled under `prefers-reduced-motion: reduce`. Speed constraint respected — no JS, no new assets.
- Wired the rating widget (`assets/js/rating.js`) into all **guide/lead-gen pages** (previously only on the 8 generators + validator). Added to the `guidePage()` template in `tools/build-pages.js`: a `.rating-widget` div at the bottom (after the sibling band) plus the `rating.js` module script. Guides have no `SoftwareApplication` block, so the widget uses `data-schema-type="CreativeWorkSeries"` with `data-schema-name="${p.crumbName}"` (the existing custom-injection mechanism) — a real `aggregateRating` is injected only when genuine votes exist (count > 0), per the no-fabricated-ratings constraint. Covers all 7 pages: what-is / types-of / how-to-add / json-ld / aggregaterating-schema / shopify / wordpress. Home (`index.html`, the hub) and `404.html` (noindex) intentionally excluded.
- New guide page `/aggregaterating-schema/` (in the **Guides** menu, positioned after JSON-LD). Educational guide on AggregateRating schema — what it is, required/recommended properties, a nested-Product JSON-LD example, Google's genuine-ratings rules (no fabricated ratings, per constraint §5), and links to the Review/Product generators + validator. Embeds `/assets/img/aggregaterating-schema.png` in a `<figure>` so readers see the star-rating rich result they can earn (first guide to use an inline image; relies on the global responsive `img` rule). Added as a new entry in the `guides` array in `tools/pages-data.js`; nav dropdown + footer links added in `tools/build-pages.js` and the 2 hand-written pages with the Guides menu (`index.html`, `schema-markup-validator/index.html`). Sitemap regenerated (16 → 17 URLs).
- Deprecation notice on `/faq-schema-generator/` (top of page, above H1) explaining Google deprecated FAQ rich results — no longer shown in Search as of May 7, 2026 (announced May 8, 2026; docs removed June 15, 2026), per https://developers.google.com/search/updates#faq-deprecation; `FAQPage` markup stays valid, tool stays fully functional. Implemented as a new optional `notice` field on the FAQ config in `tools/pages-data.js`, rendered by the generator template in `tools/build-pages.js`, styled with a new `.notice` callout (warn tokens, dark-mode aware) in `main.css`. Static/non-dismissible to respect the no-browser-storage constraint.
- Protected admin endpoint (`functions/api/admin/rating.js`) for manual rating adjustments via KV config.
- Support for `config:<page>` in `functions/api/rating.js` and frontend widget `assets/js/rating.js` to override or disable ratings.
- Wired the rating widget (`assets/js/rating.js`) into all generator tool pages via `tools/build-pages.js`.
- Added support for custom schema injection (e.g. `CreativeWorkSeries`) to `rating.js` via `data-schema-type` and `data-schema-name` attributes.

### Changed
- **`/shopify-product-schema-generator/`** — reworked the three rating inputs (average rating, review count, best rating). They now take **only the metafield path** — the `product.metafields.` prefix is hardcoded in the emitted Liquid and only the user's input is substituted (e.g. typing `reviews.rating.value.rating` yields `{{ product.metafields.reviews.rating.value.rating | json }}`). Inputs start **blank** with a single `REPLACE_WITH_YOUR_METAFIELD` placeholder (no pre-filled defaults; `DEFAULTS` is now empty). Best rating lost its plain-number/`5` fallback and is metafield-only like the others; when left blank the `bestRating` line is omitted (schema.org defaults it to 5). Labels/hints updated to explain the auto-added prefix.
- Changed the required environment variable for the admin endpoint from `ADMIN_TOKEN` to `ADMIN_SECRET_KEY`.
- Updated code to explicitly use the `ratings-variable` KV namespace binding if needed.

### Fixed
- **`/shopify-product-schema-generator/`** — removed the escaped forward slashes (`\/`) and `&` in emitted URLs/image URLs. Shopify's `| json` filter escapes `/` and `&`, which is valid JSON but reads as garbled URLs. URL and image values (product `url`, `image`, per-variant/single Offer `url`) are now output inside literal quotes without the `json` filter — these values never contain characters that need JSON escaping (no quotes/backslashes/control chars), so they stay valid while rendering clean, readable URLs. Text fields (`name`, `description`, `brand`, ratings) still use `| json`.
- Fixed a mobile responsiveness issue where the `.tool` grid would stretch horizontally off-screen due to CSS Grid blowout from the `<pre>` code block. Added `min-width: 0;` to `.panel`.
- Fixed an issue where the rating stars would not visually color yellow to reflect the current average rating.

### Added
- Wired the rating widget (`assets/js/rating.js`) into the `schema-markup-validator/index.html` hand-written page.
- `AGENTS.md` — source-of-truth onboarding doc for AI agents (constraints, architecture, workflows).
- `CHANGELOG.md` — this file.

---

## [0.1.0] — 2026-06-21 — Initial v1 build

First working version. Vanilla static site, no build step, no runtime dependencies. Builds + verifies clean (all JS parses, all 26 embedded JSON-LD blocks valid, all internal links resolve, validator engine unit-tested).

### Added — Foundation
- `assets/css/main.css` — hand-written design system. Light + dark mode via `prefers-color-scheme` and a `[data-theme]` toggle (theme is the only thing in `localStorage`). Includes nav/footer/tool/validation styles, reserved no-layout-shift ad slots, and JSON syntax-highlight tokens.
- `assets/js/shared.js` — `boot()` (theme + nav init), `toast`, `copyText`, `downloadFile`, `asScriptTag`, `debounce`, hand-rolled `highlightJson`/`renderJsonInto`, `el()` DOM helper, `prune()` (drops empty/undefined fields).
- `assets/js/schema-rules.js` — single source of truth: required/recommended properties per schema.org type (FAQPage, Product/Offer/AggregateRating/Review/Rating, Article+aliases, Person, Organization, LocalBusiness+aliases, PostalAddress, BreadcrumbList/ListItem, Event, VideoObject, SoftwareApplication). `getRule()` resolves aliases.
- `assets/js/validator.js` — validation engine. Parses JSON-LD text, extracts JSON-LD from raw HTML (via `DOMParser`), unwraps `@graph`, walks nodes against `schema-rules.js`, supports `oneOf` requirements and collection child checks. Returns levelled findings + `summarize()`.
- `LICENSE` (MIT), `.gitignore`, `CONTRIBUTING.md`, `README.md` (marketing + WebSensePro/YouTube backlinks, MIT badge).

### Added — Pages (hand-written)
- `index.html` — home/hub. WebSite + Organization JSON-LD, tool cards, guide cards, explainer copy.
- `schema-markup-validator/index.html` + `assets/js/validator-page.js` — two modes (paste JSON-LD / paste HTML), live results with red errors / amber warnings, "fix it in the generator" links, sample loader.
- `404.html` — custom, noindex, links back into the hub.

### Added — Generators (framework + 8 tools)
- `assets/js/generators/core.js` — `mountGenerator(config)`: renders form (incl. repeatable `list` fields + Advanced `<details>`), live debounced JSON-LD output with highlighting, Copy JSON / Copy `<script>` / Download buttons, inline validation via the shared engine.
- Per-type configs: `faq.js`, `product.js`, `article.js`, `local-business.js`, `organization.js`, `breadcrumb.js`, `event.js`, `review.js`. Each ships a prefilled sample. Review/Product never emit fake ratings (only when user provides real values).
- Generated pages for all 8 at their slugs, each with `BreadcrumbList` + `SoftwareApplication` JSON-LD and a 300–500-word explainer; FAQ page dogfoods a real `FAQPage`.

### Added — Guides & lead-gen (generated)
- `/what-is-schema-markup/`, `/types-of-schema-markup/`, `/how-to-add-schema-markup/`, `/json-ld/`.
- `/shopify-schema-markup/`, `/wordpress-schema-markup/` — lead-gen pages with a tasteful WebSensePro CTA.

### Added — Dev tooling (optional, not required to run site)
- `tools/build-pages.js` + `tools/pages-data.js` — generate generator/guide/lead-gen pages from shared templates (keeps nav/footer/head consistent). Committed output is plain static HTML.
- `tools/build-sitemap.js` — regenerates `sitemap.xml` (16 URLs).

### Added — Global SEO / assets
- `robots.txt`, `sitemap.xml`, `site.webmanifest`.
- `favicon.svg`, `favicon.ico`, `assets/img/og-default.svg` + generated 1200×630 `og-default.png`, `logo-512.png`, `apple-touch-icon.png`.

### Added — Ratings (opt-in, not wired in v1)
- `functions/api/rating.js` — Cloudflare Pages Function backed by KV (`RATINGS` binding), one vote per IP per page per day, never fabricates ratings.
- `assets/js/rating.js` — async, on-idle widget that injects a real `aggregateRating` only when count > 0.

### Notes / deferred
- Ratings widget deferred to v1.1 per spec — pages ship valid `SoftwareApplication` **without** `aggregateRating`.
- Video schema generator (`/video-schema-generator/`) deferred (was optional v1.5).
- Not yet done: git commit/push, GitHub repo settings, Cloudflare Pages connection, AdSense (post-approval).
- `favicon.ico` is a PNG payload (SVG favicon is primary).
